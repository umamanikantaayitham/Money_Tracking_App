import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.moneytrackerpro.app',
  appName: 'Money Tracker Pro',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
